#!/usr/bin/env ruby
#^syntax detection

forge "https://forgeapi.puppetlabs.com"

# use dependencies defined in metadata.json
# metadata

# use dependencies defined in Modulefile
# modulefile

# A module from the Puppet Forge
#mod 'puppetlabs-stdlib'

# A module from git
mod 'globalx-profiles',
   :git => 'git://github.com/rovens/puppet_profiles.git'

mod 'globalx-roles',
   :git => 'git://github.com/rovens/puppet_roles.git'
